/**
 * /inicio — la pantalla del sistema.
 *
 * La vista vive en `components/os/home-view.tsx` porque `/panel` es la MISMA
 * pantalla: dos rutas, un solo sitio donde arreglarla.
 */
export { HomeView as default } from "@/components/os/home-view";
